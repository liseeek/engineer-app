# Copy to .env for local `npm start` (Create React App reads this at dev server start).
# Must align with backend MEDHUB_DOCTOR_SELF_SIGNUP_ENABLED when testing doctor registration.
REACT_APP_DOCTOR_SIGNUP_ENABLED=true
